const reducer = "reducer";

export const agentTypesSelector = state => state[reducer].agentTypes;
export const performativesSelector = state => state[reducer].performatives;
export const agentsSelector = state => state[reducer].agents;
